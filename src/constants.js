// src/constants.js

/**
 * Directory where command modules are located.
 * @type {string}
 */
export const COMMANDS_DIR = './src/commands';

/**
 * The primary configuration file name for the application.
 * @type {string}
 */
export const CONFIG_FILE_NAME = 'prodcat.config.js';

/**
 * The name of the RC (runtime configuration) file.
 * @type {string}
 */
export const RC_FILE_NAME = '.prodcatrc.js';

/**
 * The name of the package.json file.
 * @type {string}
 */
export const PACKAGE_JSON = 'package.json';
